<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.administrator.test">

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <uses-permission android:name="android.permission.INTERNET" />


    -
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.Holo.Light">


        -
        <activity
            android:name=".individuation.LoginActivity"
            android:label="@string/app_name">


            -
            <intent-filter>

                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />

            </intent-filter>

        </activity>

        <activity
            android:name=".individuation.RegisterActivity"
            android:label="@string/title_activity_register"></activity>

        <activity
            android:name=".individuation.IndexActivity"
            android:label="@string/title_activity_index"></activity>

    <!-- <activity
            android:name=".individuation.HotFragment"
            android:label="@string/title_activity_hot_fragment"></activity>-->

        <activity
            android:name=".action.PersonalActivity"
            android:label="@string/title_activity_personal"></activity>

        <activity
            android:name=".action.ChangeActivity"
            android:label="@string/title_activity_change"></activity>

        <activity
            android:name=".admin.ShowAllUserActivity"
            android:label="@string/title_activity_show_all_user"></activity>

        <activity
            android:name=".simallutil.WeatherActivity"
            android:label="@string/title_activity_weather"></activity>

        <activity
            android:name=".action.AboutActivity"
            android:label="@string/title_activity_about"></activity>

        <activity
            android:name=".action.ChangeCityActivity"
            android:label="@string/title_activity_change_city"></activity>

    </application>

</manifest>